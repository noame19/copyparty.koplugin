-- _meta.lua — 插件元数据
-- Copyparty plugin for KOReader
-- License: MIT (same as copyparty upstream)

local _ = require("gettext")

return {
    fullname = _("Copyparty"),
    description = _([[Turn your e-reader into a file server (HTTP + WebDAV + FTP) using bundled copyparty Python script.]]),
}